name: workflow-ci-mlflow

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: wine_processed.pkl}
    command: "python modelling.py --data_path {data_path}"
